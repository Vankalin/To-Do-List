
const initState = {
	user:{ 
			login: '',
			password: '',
			
		},
	
};

const signReducer = (state = initState, action) => {
	switch (action.type) {
		
		default:
			return state;
	}
};
export default signReducer;
